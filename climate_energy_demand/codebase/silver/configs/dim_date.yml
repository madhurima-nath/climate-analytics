# silver/configs/dim_date.yml
target_table: "silver.dim_date"
description: "Master calendar"

sources: {} # No source table needed, it's generated by code

module: "common"
function: "create_dim_date"

# These match the 'params' dictionary in the Python function
params:
  start_date: "2010-01-01"
  end_date: "2050-12-31"